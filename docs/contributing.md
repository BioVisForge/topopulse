# Contributing

See the repository `CONTRIBUTING.md`. New adapters must define identifier and missing-data
behavior explicitly. Numeric changes need manually verifiable tests against small examples.
Performance claims require checked-in benchmark code and recorded environment metadata.

